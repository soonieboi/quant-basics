[build]
builder = "RAILPACK"

[deploy]
startCommand = "sh -c 'streamlit run dashboard.py --server.address 0.0.0.0 --server.port ${PORT:-8501}'"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
